<Window x:Class="WidgetDashboard.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Widget Dashboard" Height="600" Width="900"
        WindowStartupLocation="CenterScreen"
        Background="#1E1E1E">
    
    <Window.Resources>
        <!-- Modern Button Style -->
        <Style x:Key="ModernButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#2D2D30"/>
            <Setter Property="Foreground" Value="#FFFFFF"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="BorderBrush" Value="#3F3F46"/>
            <Setter Property="Padding" Value="12,8"/>
            <Setter Property="Margin" Value="5"/>
            <Setter Property="FontFamily" Value="Segoe UI"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="6">
                            <ContentPresenter HorizontalAlignment="Center"
                                            VerticalAlignment="Center"
                                            Margin="{TemplateBinding Padding}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#404040"/>
                                <Setter Property="BorderBrush" Value="#007ACC"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="#007ACC"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Card Style -->
        <Style x:Key="CardStyle" TargetType="Border">
            <Setter Property="Background" Value="#252526"/>
            <Setter Property="CornerRadius" Value="8"/>
            <Setter Property="Padding" Value="15"/>
            <Setter Property="Margin" Value="5"/>
            <Setter Property="BorderBrush" Value="#3F3F46"/>
            <Setter Property="BorderThickness" Value="1"/>
        </Style>

        <!-- Header Style -->
        <Style x:Key="HeaderTextStyle" TargetType="TextBlock">
            <Setter Property="FontFamily" Value="Segoe UI"/>
            <Setter Property="FontSize" Value="18"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Foreground" Value="#FFFFFF"/>
            <Setter Property="Margin" Value="0,0,0,10"/>
        </Style>

        <!-- Subheader Style -->
        <Style x:Key="SubheaderTextStyle" TargetType="TextBlock">
            <Setter Property="FontFamily" Value="Segoe UI"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="Normal"/>
            <Setter Property="Foreground" Value="#CCCCCC"/>
            <Setter Property="Margin" Value="0,0,0,5"/>
        </Style>

        <!-- Status Style -->
        <Style x:Key="StatusTextStyle" TargetType="TextBlock">
            <Setter Property="FontFamily" Value="Segoe UI"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="FontWeight" Value="Normal"/>
            <Setter Property="Foreground" Value="#888888"/>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Grid.Row="0" Background="#2D2D30" Padding="20,15">
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="⚡" FontSize="24" Foreground="#007ACC" Margin="0,0,15,0" VerticalAlignment="Center"/>
                <TextBlock Text="Widget Dashboard" 
                           Style="{StaticResource HeaderTextStyle}"
                           FontSize="24"
                           Margin="0"/>
                <TextBlock Text="Manage your desktop widgets" 
                           Style="{StaticResource StatusTextStyle}"
                           Margin="20,5,0,0"
                           VerticalAlignment="Center"/>
            </StackPanel>
        </Border>

        <!-- Main Content -->
        <Grid Grid.Row="1" Margin="20">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="1*"/>
                <ColumnDefinition Width="2*"/>
            </Grid.ColumnDefinitions>

            <!-- Available Widgets Panel -->
            <Border Grid.Column="0" Style="{StaticResource CardStyle}">
                <StackPanel>
                    <TextBlock Text="📦 Available Widgets" Style="{StaticResource HeaderTextStyle}"/>
                    <TextBlock Text="Click to launch widgets" Style="{StaticResource StatusTextStyle}" Margin="0,0,0,15"/>
                    
                    <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
                        <ItemsControl ItemsSource="{Binding AvailableWidgets}">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Border Background="#2D2D30" 
                                           CornerRadius="6" 
                                           Padding="12" 
                                           Margin="0,0,0,10"
                                           BorderBrush="#3F3F46"
                                           BorderThickness="1">
                                        <StackPanel>
                                            <TextBlock Text="{Binding Name}" 
                                                      Style="{StaticResource SubheaderTextStyle}"
                                                      FontWeight="Medium"/>
                                            <TextBlock Text="{Binding Description}" 
                                                      Style="{StaticResource StatusTextStyle}"
                                                      TextWrapping="Wrap"
                                                      Margin="0,0,0,10"/>
                                            <Button Content="🚀 Launch Widget" 
                                                   Style="{StaticResource ModernButtonStyle}"
                                                   Click="LaunchWidget_Click"
                                                   Tag="{Binding}"/>
                                        </StackPanel>
                                    </Border>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </ScrollViewer>
                </StackPanel>
            </Border>

            <!-- Active Widgets Panel -->
            <Border Grid.Column="1" Style="{StaticResource CardStyle}" Margin="15,5,5,5">
                <StackPanel>
                    <TextBlock Text="🎯 Active Widgets" Style="{StaticResource HeaderTextStyle}"/>
                    <TextBlock Text="Manage running widgets" Style="{StaticResource StatusTextStyle}" Margin="0,0,0,15"/>
                    
                    <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
                        <ItemsControl ItemsSource="{Binding ActiveWidgets}">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Border Background="#2D2D30" 
                                           CornerRadius="6" 
                                           Padding="12" 
                                           Margin="0,0,0,10"
                                           BorderBrush="#3F3F46"
                                           BorderThickness="1">
                                        <Grid>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="*"/>
                                                <ColumnDefinition Width="Auto"/>
                                            </Grid.ColumnDefinitions>
                                            
                                            <StackPanel Grid.Column="0">
                                                <TextBlock Text="{Binding Name}" 
                                                          Style="{StaticResource SubheaderTextStyle}"
                                                          FontWeight="Medium"/>
                                                <TextBlock Text="{Binding IsRunning, Converter={StaticResource BoolToStatusConverter}}" 
                                                          Style="{StaticResource StatusTextStyle}"/>
                                            </StackPanel>
                                            
                                            <StackPanel Grid.Column="1" Orientation="Horizontal">
                                                <Button Content="👁️ Show" 
                                                       Style="{StaticResource ModernButtonStyle}"
                                                       Click="ShowWidget_Click"
                                                       Tag="{Binding}"
                                                       Margin="0,0,5,0"/>
                                                <Button Content="🙈 Hide" 
                                                       Style="{StaticResource ModernButtonStyle}"
                                                       Click="HideWidget_Click"
                                                       Tag="{Binding}"
                                                       Margin="0,0,5,0"/>
                                                <Button Content="🛑 Stop" 
                                                       Style="{StaticResource ModernButtonStyle}"
                                                       Click="StopWidget_Click"
                                                       Tag="{Binding}"/>
                                            </StackPanel>
                                        </Grid>
                                    </Border>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </ScrollViewer>
                    
                    <!-- Stop All Button -->
                    <Separator Margin="0,15,0,10" Background="#3F3F46"/>
                    <Button Content="🛑 Stop All Widgets" 
                           Style="{StaticResource ModernButtonStyle}"
                           Background="#8B0000"
                           Click="StopAllWidgets_Click"
                           HorizontalAlignment="Stretch"/>
                </StackPanel>
            </Border>
        </Grid>
    </Grid>
</Window>
